// SFMLGame.cpp : Defines the entry point for the console application.
//

#include <SFML/Graphics.hpp>
#include "stdafx.h"
#include "GameObject.h"
#include <iostream>
#include "SceneGraph.h"

//using namespace std;

/*
float centerX = 960;
float centerY = 540;
bool AmIExiting = false;
enum GameState { Uninitialized, ShowingSplash, Paused, ShowingMenu, Playing, Exiting };
static GameState _gameState;
static sf::RenderWindow window;

		float GetCenterX() { return centerX; }
		void SetCenterX(float x) { centerX = x; }
		float GetCenterY() { return centerY; }
		void SetCenterY(float y) { centerY = y; }
		bool GetAmIExiting() { return AmIExiting; }
		void SetAmIExiting(float val) { AmIExiting = val; }
		GameState GetGameState() { return _gameState; }
		void SetGameState(GameState gam) { _gameState = gam; }
*/

//void DoEverything();
