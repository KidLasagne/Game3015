#pragma once

#include <iostream>
#include <vector>
#include <SFML/Graphics.hpp>

/*

using namespace std;

class ScalarProduct
{
public:
	ScalarProduct(sf::Vector2f a, sf::Vector2f b);

	double scalar_product(sf::Vector2f a, sf::Vector2f b)
	{
		double product = 0;
		for (int i = 0; i <= a    .size() - 1; i++)
			for (int i = 0; i <= b.size() - 1; i++)
				product = product + (a[i])*(b[i]);
		return product;
	}
};

*/